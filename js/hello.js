//  FALSY AND TRUTHY

let data = "";
if (data) {
    document.writeln("TRUE");
} else {
    document.writeln("FALSE");
}

/**
 * FALSY  adalah value yang ketika dalam kontext boolean, dia dianggap false
 * truthy adalah kebalikan atau negasi dari data falsy.
 * DATA FALSY :: (false, 0,-0, "",'',``, null, undefined, NaN)
 */